{
	"name": "kreuzberg-wasm-cloudflare-workers",
	"description": "Cloudflare Workers WASM example with Kreuzberg document intelligence",
	"version": "4.0.0-rc.10",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "wrangler dev --ip 127.0.0.1",
		"deploy": "wrangler deploy",
		"typecheck": "tsc --noEmit",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write ."
	},
	"dependencies": {
		"@kreuzberg/wasm": "workspace:*"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20251216.0",
		"@types/node": "^25.0.2",
		"prettier": "^3.7.4",
		"typescript": "^5.9.3",
		"wrangler": "^4.54.0"
	}
}
